machine:
  services:
    - docker

dependencies:
  override:
    - touch deploy/id_rsa.pub
    - make build
    - make run

test:
  override:
    - curl -v localhost:8080
    - curl -s localhost:8080 | grep RSVP


